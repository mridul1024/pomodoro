import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  timeContainer: {
    width: "100%",
    height: "20vh",
  },
  text: {
    color: "white",
    fontSize: "18px",
  },
  buttonContainer: {
    display: "flex",
    flexDirection: "column",
  },
});
